import { EventEmitter } from '@angular/core';
import { Toolbar } from './toolbar';
import { ToolbarMenuItem } from './toolbar-menu-item';
export declare class ToolbarComponent {
    config: Toolbar;
    onClick: EventEmitter<ToolbarMenuItem>;
}
